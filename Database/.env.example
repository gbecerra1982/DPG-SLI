# ============================================
# COSMOS DB CONFIGURATION - AZURE AD AUTH
# ============================================
#
# ⚠️ IMPORTANTE: Este es solo un archivo de ejemplo
# El archivo .env real debe estar en la RAÍZ del proyecto
# Ubicación correcta: c:\workspaces\DPG\SLI\.env
# Ubicación incorrecta: c:\workspaces\DPG\SLI\Database\.env
#
# Los scripts en /Database están configurados para leer desde la raíz
# NUNCA commitear .env al repositorio
#
# ============================================

# ⚠️ AUTENTICACIÓN: Este proyecto usa Azure AD / Managed Identity
# No se requiere COSMOS_KEY - La autenticación se realiza via DefaultAzureCredential
#
# DefaultAzureCredential intenta estas credenciales en orden:
# 1. Variables de entorno (AZURE_TENANT_ID, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET)
# 2. Managed Identity (cuando se ejecuta en Azure VM, App Service, Functions, etc)
# 3. Visual Studio Code (usuario autenticado)
# 4. Azure CLI (az login)
# 5. Azure PowerShell
#
# Para desarrollo local: Ejecutar 'az login' antes de correr los scripts
#
# ============================================

# Cosmos DB Endpoint (URI)
# Obtenerlo de: Azure Portal → Cosmos DB Account → Overview → URI
COSMOS_ENDPOINT=https://YOUR_ACCOUNT_NAME.documents.azure.com:443/

# Database Name (opcional - usa default "SLIDB")
COSMOS_DATABASE_ID=SLIDB

# ============================================
# OPCIONAL: CONFIGURACIÓN ADICIONAL
# ============================================

# Nivel de logging
# LOG_LEVEL=info

# Timeout para conexiones (ms)
# COSMOS_TIMEOUT=30000

# Región preferida
# COSMOS_PREFERRED_REGION=East US
